// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'json_card_theme_builder.dart';

// **************************************************************************
// Generator: JsonWidgetLibraryBuilder
// **************************************************************************

// ignore_for_file: avoid_init_to_null
// ignore_for_file: deprecated_member_use

// ignore_for_file: prefer_const_constructors
// ignore_for_file: prefer_const_constructors_in_immutables
// ignore_for_file: prefer_final_locals
// ignore_for_file: prefer_if_null_operators
// ignore_for_file: prefer_single_quotes
// ignore_for_file: unused_local_variable

class JsonCardThemeBuilder extends _JsonCardThemeBuilder {
  const JsonCardThemeBuilder({required super.args});

  static const kType = 'card_theme';

  /// Constant that can be referenced for the builder's type.
  @override
  String get type => kType;

  /// Static function that is capable of decoding the widget from a dynamic JSON
  /// or YAML set of values.
  static JsonCardThemeBuilder fromDynamic(
    dynamic map, {
    JsonWidgetRegistry? registry,
  }) => JsonCardThemeBuilder(args: map);

  @override
  JsonCardThemeBuilderModel createModel({
    ChildWidgetBuilder? childBuilder,
    required JsonWidgetData data,
  }) {
    final model = JsonCardThemeBuilderModel.fromDynamic(
      args,
      registry: data.jsonWidgetRegistry,
    );

    return model;
  }

  @override
  CardTheme buildCustom({
    ChildWidgetBuilder? childBuilder,
    required BuildContext context,
    required JsonWidgetData data,
    Key? key,
  }) {
    final model = createModel(childBuilder: childBuilder, data: data);

    return CardTheme(
      clipBehavior: model.clipBehavior,
      color: model.color,
      data: model.data,
      elevation: model.elevation,
      key: key,
      margin: model.margin,
      shadowColor: model.shadowColor,
      shape: model.shape,
      surfaceTintColor: model.surfaceTintColor,
      child: model.child?.build(childBuilder: childBuilder, context: context),
    );
  }
}

class JsonCardTheme extends JsonWidgetData {
  JsonCardTheme({
    Map<String, dynamic> args = const {},
    JsonWidgetRegistry? registry,
    this.clipBehavior,
    this.color,
    this.data,
    this.elevation,
    this.margin,
    this.shadowColor,
    this.shape,
    this.surfaceTintColor,
    this.child,
  }) : super(
         jsonWidgetArgs: JsonCardThemeBuilderModel.fromDynamic(
           {
             'clipBehavior': clipBehavior,
             'color': color,
             'data': data,
             'elevation': elevation,
             'margin': margin,
             'shadowColor': shadowColor,
             'shape': shape,
             'surfaceTintColor': surfaceTintColor,
             'child': child,

             ...args,
           },
           args: args,
           registry: registry,
         ),
         jsonWidgetBuilder:
             () => JsonCardThemeBuilder(
               args: JsonCardThemeBuilderModel.fromDynamic(
                 {
                   'clipBehavior': clipBehavior,
                   'color': color,
                   'data': data,
                   'elevation': elevation,
                   'margin': margin,
                   'shadowColor': shadowColor,
                   'shape': shape,
                   'surfaceTintColor': surfaceTintColor,
                   'child': child,

                   ...args,
                 },
                 args: args,
                 registry: registry,
               ),
             ),
         jsonWidgetType: JsonCardThemeBuilder.kType,
       );

  final Clip? clipBehavior;

  final Color? color;

  final CardThemeData? data;

  final double? elevation;

  final EdgeInsetsGeometry? margin;

  final Color? shadowColor;

  final ShapeBorder? shape;

  final Color? surfaceTintColor;

  final JsonWidgetData? child;
}

/* AUTOGENERATED FROM [CardTheme]*/
/// Creates a theme that can be used for [ThemeData.cardTheme].
///
/// The [elevation] must be null or non-negative.
class JsonCardThemeBuilderModel extends JsonWidgetBuilderModel {
  const JsonCardThemeBuilderModel(
    super.args, {
    this.clipBehavior,
    this.color,
    this.data,
    this.elevation,
    this.margin,
    this.shadowColor,
    this.shape,
    this.surfaceTintColor,
    this.child,
  });

  final Clip? clipBehavior;

  final Color? color;

  final CardThemeData? data;

  final double? elevation;

  final EdgeInsetsGeometry? margin;

  final Color? shadowColor;

  final ShapeBorder? shape;

  final Color? surfaceTintColor;

  final JsonWidgetData? child;

  static JsonCardThemeBuilderModel fromDynamic(
    dynamic map, {
    Map<String, dynamic> args = const {},
    JsonWidgetRegistry? registry,
  }) {
    final result = maybeFromDynamic(map, args: args, registry: registry);

    if (result == null) {
      throw Exception(
        '[JsonCardThemeBuilder]: requested to parse from dynamic, but the input is null.',
      );
    }

    return result;
  }

  static JsonCardThemeBuilderModel? maybeFromDynamic(
    dynamic map, {
    Map<String, dynamic> args = const {},
    JsonWidgetRegistry? registry,
  }) {
    JsonCardThemeBuilderModel? result;

    if (map != null) {
      if (map is String) {
        map = yaon.parse(map, normalize: true);
      }

      if (map is JsonCardThemeBuilderModel) {
        result = map;
      } else {
        registry ??= JsonWidgetRegistry.instance;
        map = registry.processArgs(map, <String>{}).value;
        result = JsonCardThemeBuilderModel(
          args,
          clipBehavior: () {
            dynamic parsed = ThemeDecoder.decodeClip(
              map['clipBehavior'],
              validate: false,
            );

            return parsed;
          }(),
          color: () {
            dynamic parsed = ThemeDecoder.decodeColor(
              map['color'],
              validate: false,
            );

            return parsed;
          }(),
          data: () {
            dynamic parsed = ThemeDecoder.decodeCardThemeData(
              map['data'],
              validate: false,
            );

            return parsed;
          }(),
          elevation: () {
            dynamic parsed = JsonClass.maybeParseDouble(map['elevation']);

            return parsed;
          }(),
          margin: () {
            dynamic parsed = ThemeDecoder.decodeEdgeInsetsGeometry(
              map['margin'],
              validate: false,
            );

            return parsed;
          }(),
          shadowColor: () {
            dynamic parsed = ThemeDecoder.decodeColor(
              map['shadowColor'],
              validate: false,
            );

            return parsed;
          }(),
          shape: () {
            dynamic parsed = ThemeDecoder.decodeShapeBorder(
              map['shape'],
              validate: false,
            );

            return parsed;
          }(),
          surfaceTintColor: () {
            dynamic parsed = ThemeDecoder.decodeColor(
              map['surfaceTintColor'],
              validate: false,
            );

            return parsed;
          }(),
          child: () {
            dynamic parsed = JsonWidgetData.maybeFromDynamic(
              map['child'],
              registry: registry,
            );

            return parsed;
          }(),
        );
      }
    }

    return result;
  }

  @override
  Map<String, dynamic> toJson() {
    return JsonClass.removeNull({
      'clipBehavior': ThemeEncoder.encodeClip(clipBehavior),
      'color': ThemeEncoder.encodeColor(color),
      'data': ThemeEncoder.encodeCardThemeData(data),
      'elevation': elevation,
      'margin': ThemeEncoder.encodeEdgeInsetsGeometry(margin),
      'shadowColor': ThemeEncoder.encodeColor(shadowColor),
      'shape': ThemeEncoder.encodeShapeBorder(shape),
      'surfaceTintColor': ThemeEncoder.encodeColor(surfaceTintColor),
      'child': child?.toJson(),

      ...args,
    });
  }
}

class CardThemeSchema {
  static const id =
      'https://peiffer-innovations.github.io/flutter_json_schemas/schemas/json_dynamic_widget/card_theme.json';

  static final schema = <String, Object>{
    r'$schema': 'http://json-schema.org/draft-07/schema#',
    r'$id': id,
    'title': 'CardTheme',
    'type': 'object',
    'additionalProperties': false,
    'properties': {
      'clipBehavior': SchemaHelper.objectSchema(ClipSchema.id),
      'color': SchemaHelper.objectSchema(ColorSchema.id),
      'data': SchemaHelper.objectSchema(CardThemeDataSchema.id),
      'elevation': SchemaHelper.numberSchema,
      'margin': SchemaHelper.objectSchema(EdgeInsetsGeometrySchema.id),
      'shadowColor': SchemaHelper.objectSchema(ColorSchema.id),
      'shape': SchemaHelper.objectSchema(ShapeBorderSchema.id),
      'surfaceTintColor': SchemaHelper.objectSchema(ColorSchema.id),
      'child': SchemaHelper.objectSchema(JsonWidgetDataSchema.id),
    },
    'required': [],
  };
}
